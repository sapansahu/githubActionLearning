name: Outflow Information
on: workflow_dispatch
jobs:
    info:
        runs-on: windows-latest
        steps:
            - name: output Github context
              run: echo "${{ toJson(github) }}"